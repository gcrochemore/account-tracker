<%= form_for(@account_line) do |f| %>
  <% if @account_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account_line.errors.count, "error") %> prohibited this account_line from being saved:</h2>

      <ul>
      <% @account_line.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :import %><br />
    <%= f.text_field :import %>
  </div>
  <div class="field">
    <%= f.label :import_line %><br />
    <%= f.text_field :import_line %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
