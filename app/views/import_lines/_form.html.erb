<%= form_for(@import_line) do |f| %>
  <% if @import_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@import_line.errors.count, "error") %> prohibited this import_line from being saved:</h2>

      <ul>
      <% @import_line.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
